{
  "name": "@neotales/os-workspace",
  "private": true,
  "description": "Platform bindings such as keychain, DPAPI, and the Windows registry",
  "scripts": {
    "build": "deno task build",
    "test": "deno task test",
    "lint": "deno task lint",
    "fmt": "deno task fmt",
    "fmt:check": "deno task fmt:check",
    "check": "deno task check",
    "pack": "deno task pack"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=11"
  }
}
